rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Enforce that only matching authenticated users can upload, retrieve, or delete objects inside datasets/{userId}/ path
    match /datasets/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
